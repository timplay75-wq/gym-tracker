// Точка входа для Vercel.
//
// Vercel не держит постоянный процесс и не даёт занимать порт: он вызывает
// экспортированный обработчик на каждый запрос. Express-приложение как раз
// является функцией (req, res), поэтому отдаём его напрямую — без app.listen().
//
// Подключение к MongoDB происходит в middleware внутри app.js и кэшируется
// между тёплыми вызовами (см. config/db.js).
export { default } from '../app.js';
